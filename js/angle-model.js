/* eslint-disable no-var, prefer-const */
/**
 * Auto-generated angle error model.
 *
 * Trained on: ai_shot_data_1770311757019.json
 * Features: cutAngle, spinY, power
 * Model: piecewise PolynomialFeatures(degree=3) + Ridge
 * Output clip: [-15, 15] degrees
 * Generated (UTC): 2026-02-05 17:30:12Z
 * Metrics (overall holdout test, random_state=42):
 *   rSquared: 0.607722
 *   rmse: 1.265553
 *   mae: 0.763954
 *
 * Generated by build_angle_model_nn.py
 */

// Bracket 0: power in [5.0, 7.4) (n=952, split=31, rmse=0.2093)
function __predictAngleError_b0_left(cutAngle, power) {
  return (
    7.36215972019e-08 * 1.0 +
    0.000255846415559 * (cutAngle) +
    3.68107326156e-07 * (power) +
    -4.18322850668e-07 * (Math.pow(cutAngle, 2)) +
    0.00127923205465 * (cutAngle * power) +
    1.84052871934e-06 * (Math.pow(power, 2)) +
    1.95652318778e-05 * (Math.pow(cutAngle, 3)) +
    -2.09182499359e-06 * (Math.pow(cutAngle, 2) * power) +
    0.00639616046715 * (cutAngle * Math.pow(power, 2)) +
    9.20266763578e-06 * (Math.pow(power, 3))
  );
}

function __predictAngleError_b0_right(cutAngle, power) {
  return (
    0.000360654559928 * 1.0 +
    -2.74783480637e-05 * (cutAngle) +
    0.0018032727968 * (power) +
    1.20478227007e-05 * (Math.pow(cutAngle, 2)) +
    -0.000137391810352 * (cutAngle * power) +
    0.00901636402266 * (Math.pow(power, 2)) +
    -2.20625034223e-06 * (Math.pow(cutAngle, 3)) +
    6.02268485929e-05 * (Math.pow(cutAngle, 2) * power) +
    -0.000686959979273 * (cutAngle * Math.pow(power, 2)) +
    0.0450818204956 * (Math.pow(power, 3))
  );
}

// Bracket 2: power in [9.8, 12.2) (n=1324, split=31, rmse=0.2459)
function __predictAngleError_b2_left(cutAngle, power) {
  return (
    5.70760848709e-09 * 1.0 +
    1.32439945723e-05 * (cutAngle) +
    5.96399131549e-08 * (power) +
    2.37830888578e-06 * (Math.pow(cutAngle, 2)) +
    0.000138459857541 * (cutAngle * power) +
    6.23731470986e-07 * (Math.pow(power, 2)) +
    1.25008357532e-05 * (Math.pow(cutAngle, 3)) +
    2.48614019407e-05 * (Math.pow(cutAngle, 2) * power) +
    0.00144753243336 * (cutAngle * Math.pow(power, 2)) +
    6.51989521794e-06 * (Math.pow(power, 3))
  );
}

function __predictAngleError_b2_right(cutAngle, power) {
  return (
    3.8442770938e-06 * 1.0 +
    1.98080107652e-06 * (cutAngle) +
    4.01901789398e-05 * (power) +
    -5.79060691748e-06 * (Math.pow(cutAngle, 2)) +
    2.07077357559e-05 * (cutAngle * power) +
    0.00042017035306 * (Math.pow(power, 2)) +
    4.10371563531e-06 * (Math.pow(cutAngle, 3)) +
    -6.05118985849e-05 * (Math.pow(cutAngle, 2) * power) +
    0.000216491459123 * (cutAngle * Math.pow(power, 2)) +
    0.00439267270414 * (Math.pow(power, 3))
  );
}

// Bracket 4: power in [14.6, 17.0) (n=1324, split=31, rmse=0.5104)
function __predictAngleError_b4_left(cutAngle, power) {
  return (
    3.09156089786e-09 * 1.0 +
    2.01537164344e-06 * (cutAngle) +
    4.91622586375e-08 * (power) +
    8.27692440793e-06 * (Math.pow(cutAngle, 2)) +
    3.2062736069e-05 * (cutAngle * power) +
    7.8225779946e-07 * (Math.pow(power, 2)) +
    -3.26619659235e-05 * (Math.pow(cutAngle, 3)) +
    0.000131684640516 * (Math.pow(cutAngle, 2) * power) +
    0.000510082449158 * (cutAngle * Math.pow(power, 2)) +
    1.24319015412e-05 * (Math.pow(power, 3))
  );
}

function __predictAngleError_b4_right(cutAngle, power) {
  return (
    3.43572752346e-07 * 1.0 +
    -4.53315390253e-07 * (cutAngle) +
    5.46576083588e-06 * (power) +
    4.09883264641e-07 * (Math.pow(cutAngle, 2)) +
    -7.2113630736e-06 * (cutAngle * power) +
    8.69547298334e-05 * (Math.pow(power, 2)) +
    -1.27763082137e-07 * (Math.pow(cutAngle, 3)) +
    6.37774963706e-06 * (Math.pow(cutAngle, 2) * power) +
    -0.000114741123426 * (cutAngle * Math.pow(power, 2)) +
    0.00138346524316 * (Math.pow(power, 3))
  );
}

// Bracket 6: power in [19.4, 21.8) (n=1324, split=31, rmse=0.8777)
function __predictAngleError_b6_left(cutAngle, power) {
  return (
    1.10666909386e-09 * 1.0 +
    4.48852255544e-07 * (cutAngle) +
    2.36148381239e-08 * (power) +
    9.69496396682e-06 * (Math.pow(cutAngle, 2)) +
    9.58951274258e-06 * (cutAngle * power) +
    5.04918890279e-07 * (Math.pow(power, 2)) +
    -9.04450654737e-05 * (Math.pow(cutAngle, 3)) +
    0.000207128622468 * (Math.pow(cutAngle, 2) * power) +
    0.000204863416809 * (cutAngle * Math.pow(power, 2)) +
    1.07912325851e-05 * (Math.pow(power, 3))
  );
}

function __predictAngleError_b6_right(cutAngle, power) {
  return (
    7.00509389894e-08 * 1.0 +
    -6.51055808211e-07 * (cutAngle) +
    1.49719433288e-06 * (power) +
    5.14452347983e-06 * (Math.pow(cutAngle, 2)) +
    -1.39088555582e-05 * (cutAngle * power) +
    3.19755403524e-05 * (Math.pow(power, 2)) +
    -1.67945538705e-05 * (Math.pow(cutAngle, 3)) +
    0.000109558876039 * (Math.pow(cutAngle, 2) * power) +
    -0.000296890235676 * (cutAngle * Math.pow(power, 2)) +
    0.000683374276398 * (Math.pow(power, 3))
  );
}

// Bracket 9: power in [26.599999999999998, 29.0) (n=1324, split=30, rmse=0.8865)
function __predictAngleError_b9_left(cutAngle, power) {
  return (
    7.9289321171e-11 * 1.0 +
    2.5394863844e-07 * (cutAngle) +
    2.05418536405e-09 * (power) +
    1.92004291471e-06 * (Math.pow(cutAngle, 2)) +
    6.8111033993e-06 * (cutAngle * power) +
    5.66876582525e-08 * (Math.pow(power, 2)) +
    -1.56921037811e-05 * (Math.pow(cutAngle, 3)) +
    5.14850691838e-05 * (Math.pow(cutAngle, 2) * power) +
    0.000182634305061 * (cutAngle * Math.pow(power, 2)) +
    1.5306574534e-06 * (Math.pow(power, 3))
  );
}

function __predictAngleError_b9_right(cutAngle, power) {
  return (
    1.37982016742e-08 * 1.0 +
    -3.02010489127e-09 * (cutAngle) +
    3.70252845978e-07 * (power) +
    -1.20768011424e-06 * (Math.pow(cutAngle, 2)) +
    -8.16111726078e-08 * (cutAngle * power) +
    9.93026010286e-06 * (Math.pow(power, 2)) +
    8.04240662114e-06 * (Math.pow(cutAngle, 3)) +
    -3.25622916963e-05 * (Math.pow(cutAngle, 2) * power) +
    -2.10615195483e-06 * (cutAngle * Math.pow(power, 2)) +
    0.000266062654363 * (Math.pow(power, 3))
  );
}

// Bracket 11: power in [31.4, 33.8) (n=1324, split=15, rmse=1.3018)
function __predictAngleError_b11_left(cutAngle, power) {
  return (
    -6.18315307795e-13 * 1.0 +
    1.25595861708e-07 * (cutAngle) +
    -3.23965521452e-11 * (power) +
    2.76615594314e-07 * (Math.pow(cutAngle, 2)) +
    4.05336149944e-06 * (cutAngle * power) +
    -1.21581956061e-09 * (Math.pow(power, 2)) +
    1.76655452522e-05 * (Math.pow(cutAngle, 3)) +
    8.92631250701e-06 * (Math.pow(cutAngle, 2) * power) +
    0.000130810216126 * (cutAngle * Math.pow(power, 2)) +
    -2.91039760688e-08 * (Math.pow(power, 3))
  );
}

function __predictAngleError_b11_right(cutAngle, power) {
  return (
    -2.47657907625e-09 * 1.0 +
    4.28848790838e-07 * (cutAngle) +
    -8.11692180628e-08 * (power) +
    -9.51511020514e-06 * (Math.pow(cutAngle, 2)) +
    1.38321979615e-05 * (cutAngle * power) +
    -2.61560600182e-06 * (Math.pow(power, 2)) +
    6.27623076542e-05 * (Math.pow(cutAngle, 3)) +
    -0.000308345482497 * (Math.pow(cutAngle, 2) * power) +
    0.000446321205573 * (cutAngle * Math.pow(power, 2)) +
    -8.38373503193e-05 * (Math.pow(power, 3))
  );
}

// Bracket 13: power in [36.2, 38.6) (n=1324, split=27, rmse=1.3148)
function __predictAngleError_b13_left(cutAngle, power) {
  return (
    -1.30086450272e-13 * 1.0 +
    6.73945429251e-08 * (cutAngle) +
    -2.59825117954e-11 * (power) +
    -1.36824102106e-07 * (Math.pow(cutAngle, 2)) +
    2.54298442335e-06 * (cutAngle * power) +
    -1.38014593348e-09 * (Math.pow(power, 2)) +
    2.46157767731e-05 * (Math.pow(cutAngle, 3)) +
    -5.16810339989e-06 * (Math.pow(cutAngle, 2) * power) +
    9.59458729812e-05 * (cutAngle * Math.pow(power, 2)) +
    -6.67439276917e-08 * (Math.pow(power, 3))
  );
}

function __predictAngleError_b13_right(cutAngle, power) {
  return (
    4.775706646e-10 * 1.0 +
    1.24211135074e-07 * (cutAngle) +
    1.81033190073e-08 * (power) +
    -4.91736627329e-06 * (Math.pow(cutAngle, 2)) +
    4.71146645215e-06 * (cutAngle * power) +
    6.78956591933e-07 * (Math.pow(power, 2)) +
    4.97997859974e-05 * (Math.pow(cutAngle, 3)) +
    -0.000183328821301 * (Math.pow(cutAngle, 2) * power) +
    0.000176734556425 * (cutAngle * Math.pow(power, 2)) +
    2.54482989676e-05 * (Math.pow(power, 3))
  );
}

// Bracket 15: power in [41.0, 43.4) (n=1324, split=28, rmse=1.6468)
function __predictAngleError_b15_left(cutAngle, power) {
  return (
    9.99936980456e-13 * 1.0 +
    3.8184619534e-08 * (cutAngle) +
    -6.33350380033e-11 * (power) +
    1.69586303082e-07 * (Math.pow(cutAngle, 2)) +
    1.64824156755e-06 * (cutAngle * power) +
    -5.52203635057e-10 * (Math.pow(power, 2)) +
    7.20661617105e-06 * (Math.pow(cutAngle, 3)) +
    7.33638622602e-06 * (Math.pow(cutAngle, 2) * power) +
    7.11775979611e-05 * (cutAngle * Math.pow(power, 2)) +
    -8.84856866475e-08 * (Math.pow(power, 3))
  );
}

function __predictAngleError_b15_right(cutAngle, power) {
  return (
    -5.80254889147e-10 * 1.0 +
    1.75634028307e-07 * (cutAngle) +
    -2.58167855936e-08 * (power) +
    -7.68180889409e-06 * (Math.pow(cutAngle, 2)) +
    7.55633916663e-06 * (cutAngle * power) +
    -1.10742962285e-06 * (Math.pow(power, 2)) +
    9.54351123253e-05 * (Math.pow(cutAngle, 3)) +
    -0.000333016094483 * (Math.pow(cutAngle, 2) * power) +
    0.000327461863554 * (cutAngle * Math.pow(power, 2)) +
    -4.89866916099e-05 * (Math.pow(power, 3))
  );
}

// Bracket 18: power in [48.199999999999996, 50.6) (n=1324, split=46, rmse=1.5134)
function __predictAngleError_b18_left(cutAngle, power) {
  return (
    1.07778163236e-12 * 1.0 +
    1.74809516203e-08 * (cutAngle) +
    2.65707834553e-10 * (power) +
    1.93988294342e-06 * (Math.pow(cutAngle, 2)) +
    8.5681659798e-07 * (cutAngle * power) +
    1.49827628924e-08 * (Math.pow(power, 2)) +
    -0.000125640784161 * (Math.pow(cutAngle, 3)) +
    9.44817356409e-05 * (Math.pow(cutAngle, 2) * power) +
    4.15801309006e-05 * (cutAngle * Math.pow(power, 2)) +
    1.20877922808e-07 * (Math.pow(power, 3))
  );
}

function __predictAngleError_b18_right(cutAngle, power) {
  return (
    -4.47830984667e-11 * 1.0 +
    6.12587723641e-08 * (cutAngle) +
    -3.63809125326e-09 * (power) +
    -3.26149933559e-06 * (Math.pow(cutAngle, 2)) +
    2.9526984218e-06 * (cutAngle * power) +
    -1.56913612202e-07 * (Math.pow(power, 2)) +
    4.39819205505e-05 * (Math.pow(cutAngle, 3)) +
    -0.000159159323359 * (Math.pow(cutAngle, 2) * power) +
    0.000143065747034 * (cutAngle * Math.pow(power, 2)) +
    -8.84819812044e-06 * (Math.pow(power, 3))
  );
}

// Bracket 20: power in [53.0, 55.4) (n=1324, split=28, rmse=1.4546)
function __predictAngleError_b20_left(cutAngle, power) {
  return (
    1.21534697236e-12 * 1.0 +
    1.46803550834e-08 * (cutAngle) +
    7.09078962593e-11 * (power) +
    9.53873328159e-08 * (Math.pow(cutAngle, 2)) +
    7.95119117185e-07 * (cutAngle * power) +
    3.3872873932e-09 * (Math.pow(power, 2)) +
    1.51942810497e-05 * (Math.pow(cutAngle, 3)) +
    5.16785553641e-06 * (Math.pow(cutAngle, 2) * power) +
    4.29456109755e-05 * (cutAngle * Math.pow(power, 2)) +
    3.58426777659e-08 * (Math.pow(power, 3))
  );
}

function __predictAngleError_b20_right(cutAngle, power) {
  return (
    3.49808257554e-12 * 1.0 +
    3.41374362017e-08 * (cutAngle) +
    1.94069682253e-09 * (power) +
    -3.04140113069e-06 * (Math.pow(cutAngle, 2)) +
    1.9418260193e-06 * (cutAngle * power) +
    2.99985769277e-08 * (Math.pow(power, 2)) +
    6.3926016459e-05 * (Math.pow(cutAngle, 3)) +
    -0.00015912898324 * (Math.pow(cutAngle, 2) * power) +
    9.83430193375e-05 * (cutAngle * Math.pow(power, 2)) +
    8.10208279715e-06 * (Math.pow(power, 3))
  );
}

// Bracket 22: power in [57.8, 60.199999999999996) (n=1324, split=18, rmse=1.7726)
function __predictAngleError_b22_left(cutAngle, power) {
  return (
    -2.24100896952e-13 * 1.0 +
    1.0767185432e-08 * (cutAngle) +
    3.54189010951e-12 * (power) +
    -1.42235312691e-07 * (Math.pow(cutAngle, 2)) +
    6.41266473038e-07 * (cutAngle * power) +
    7.11991230972e-10 * (Math.pow(power, 2)) +
    3.0051616198e-05 * (Math.pow(cutAngle, 3)) +
    -8.46749758343e-06 * (Math.pow(cutAngle, 2) * power) +
    3.81886743308e-05 * (cutAngle * Math.pow(power, 2)) +
    -2.28392604448e-08 * (Math.pow(power, 3))
  );
}

function __predictAngleError_b22_right(cutAngle, power) {
  return (
    -8.97011203932e-11 * 1.0 +
    4.56925643175e-08 * (cutAngle) +
    -6.95150627419e-09 * (power) +
    -3.36218907712e-06 * (Math.pow(cutAngle, 2)) +
    2.71691317289e-06 * (cutAngle * power) +
    -4.06028432649e-07 * (Math.pow(power, 2)) +
    6.55352846857e-05 * (Math.pow(cutAngle, 3)) +
    -0.000202535177283 * (Math.pow(cutAngle, 2) * power) +
    0.000159815829576 * (cutAngle * Math.pow(power, 2)) +
    -1.97926128585e-05 * (Math.pow(power, 3))
  );
}

// Bracket 24: power in [62.599999999999994, 65.000000001) (n=1324, split=10, rmse=1.7832)
function __predictAngleError_b24_left(cutAngle, power) {
  return (
    -1.61961674526e-13 * 1.0 +
    7.38986641302e-09 * (cutAngle) +
    -9.2271754744e-12 * (power) +
    -5.21683108441e-07 * (Math.pow(cutAngle, 2)) +
    4.8035722999e-07 * (cutAngle * power) +
    -8.41694430798e-10 * (Math.pow(power, 2)) +
    0.000198638641707 * (Math.pow(cutAngle, 3)) +
    -3.39093827649e-05 * (Math.pow(cutAngle, 2) * power) +
    3.12231750282e-05 * (cutAngle * Math.pow(power, 2)) +
    -1.9342933714e-08 * (Math.pow(power, 3))
  );
}

function __predictAngleError_b24_right(cutAngle, power) {
  return (
    -4.50192765088e-11 * 1.0 +
    2.95456025924e-08 * (cutAngle) +
    -2.8032245598e-09 * (power) +
    -2.81622584792e-06 * (Math.pow(cutAngle, 2)) +
    1.92223500876e-06 * (cutAngle * power) +
    -1.98840998055e-07 * (Math.pow(power, 2)) +
    6.85096962635e-05 * (Math.pow(cutAngle, 3)) +
    -0.000183774046816 * (Math.pow(cutAngle, 2) * power) +
    0.000124242807604 * (cutAngle * Math.pow(power, 2)) +
    -1.21180487573e-05 * (Math.pow(power, 3))
  );
}

/**
 * Predict the angle error for a shot
 * @param {number} cutAngle - Cut angle in degrees (0 = straight, 90 = max)
 * @param {number} spinY - Vertical spin (-1 to 1, positive = topspin)
 * @param {number} power - Shot power
 * @returns {number} Predicted angle error in degrees
 */
function predictAngleError(cutAngle, spinY, power) {
  // basic input sanitization (avoid NaN/Infinity propagating)
  if (!Number.isFinite(cutAngle) || !Number.isFinite(spinY) || !Number.isFinite(power)) return 0;

  // NOTE: spinY is accepted for compatibility but not used by this model.

  if (power >= 5.0 && power < 7.4) {
    const split = 31;
    let y = (cutAngle < split)
      ? __predictAngleError_b0_left(cutAngle, power)
      : __predictAngleError_b0_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
  else if (power >= 9.8 && power < 12.2) {
    const split = 31;
    let y = (cutAngle < split)
      ? __predictAngleError_b2_left(cutAngle, power)
      : __predictAngleError_b2_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
  else if (power >= 14.6 && power < 17.0) {
    const split = 31;
    let y = (cutAngle < split)
      ? __predictAngleError_b4_left(cutAngle, power)
      : __predictAngleError_b4_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
  else if (power >= 19.4 && power < 21.8) {
    const split = 31;
    let y = (cutAngle < split)
      ? __predictAngleError_b6_left(cutAngle, power)
      : __predictAngleError_b6_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
  else if (power >= 26.599999999999998 && power < 29.0) {
    const split = 30;
    let y = (cutAngle < split)
      ? __predictAngleError_b9_left(cutAngle, power)
      : __predictAngleError_b9_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
  else if (power >= 31.4 && power < 33.8) {
    const split = 15;
    let y = (cutAngle < split)
      ? __predictAngleError_b11_left(cutAngle, power)
      : __predictAngleError_b11_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
  else if (power >= 36.2 && power < 38.6) {
    const split = 27;
    let y = (cutAngle < split)
      ? __predictAngleError_b13_left(cutAngle, power)
      : __predictAngleError_b13_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
  else if (power >= 41.0 && power < 43.4) {
    const split = 28;
    let y = (cutAngle < split)
      ? __predictAngleError_b15_left(cutAngle, power)
      : __predictAngleError_b15_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
  else if (power >= 48.199999999999996 && power < 50.6) {
    const split = 46;
    let y = (cutAngle < split)
      ? __predictAngleError_b18_left(cutAngle, power)
      : __predictAngleError_b18_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
  else if (power >= 53.0 && power < 55.4) {
    const split = 28;
    let y = (cutAngle < split)
      ? __predictAngleError_b20_left(cutAngle, power)
      : __predictAngleError_b20_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
  else if (power >= 57.8 && power < 60.199999999999996) {
    const split = 18;
    let y = (cutAngle < split)
      ? __predictAngleError_b22_left(cutAngle, power)
      : __predictAngleError_b22_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
  else if (power >= 62.599999999999994 && power < 65.000000001) {
    const split = 10;
    let y = (cutAngle < split)
      ? __predictAngleError_b24_left(cutAngle, power)
      : __predictAngleError_b24_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
  // fallback: clamp to nearest bracket
  if (power < 5.0) {
    const split = 31;
    let y = (cutAngle < split)
      ? __predictAngleError_b0_left(cutAngle, power)
      : __predictAngleError_b0_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
  {
    const split = 10;
    let y = (cutAngle < split)
      ? __predictAngleError_b24_left(cutAngle, power)
      : __predictAngleError_b24_right(cutAngle, power);
    if (y > 15) y = 15;
    else if (y < -15) y = -15;
    return y;
  }
}

/**
 * Calculate aim adjustment to compensate for predicted angle error
 * @param {number} cutAngle - Cut angle in degrees
 * @param {number} spinY - Vertical spin
 * @param {number} power - Shot power
 * @returns {number} Angle adjustment in degrees (subtract from aim)
 */
function calculateAimAdjustment(cutAngle, spinY, power) {
  return predictAngleError(cutAngle, spinY, power);
}

// Model metadata
const ANGLE_MODEL_INFO = {
  modelType: "piecewise_poly_ridge",
  degree: 3,
  clip: 15,
  nSamples: 15516,
  features: ["cutAngle", "spinY", "power"],
  piecewise: {
    brackets: [
      { pmin: 5.0, pmax: 7.4, split: 31, n: 952 },
      { pmin: 9.8, pmax: 12.2, split: 31, n: 1324 },
      { pmin: 14.6, pmax: 17.0, split: 31, n: 1324 },
      { pmin: 19.4, pmax: 21.8, split: 31, n: 1324 },
      { pmin: 26.599999999999998, pmax: 29.0, split: 30, n: 1324 },
      { pmin: 31.4, pmax: 33.8, split: 15, n: 1324 },
      { pmin: 36.2, pmax: 38.6, split: 27, n: 1324 },
      { pmin: 41.0, pmax: 43.4, split: 28, n: 1324 },
      { pmin: 48.199999999999996, pmax: 50.6, split: 46, n: 1324 },
      { pmin: 53.0, pmax: 55.4, split: 28, n: 1324 },
      { pmin: 57.8, pmax: 60.199999999999996, split: 18, n: 1324 },
      { pmin: 62.599999999999994, pmax: 65.000000001, split: 10, n: 1324 },
    ]
  },
  metrics: {
    rSquared: 0.607722,
    rmse: 1.265553,
    mae: 0.763954
  }
};

// Export for use in modules
if (typeof module !== "undefined" && module.exports) {
  module.exports = { predictAngleError, calculateAimAdjustment, ANGLE_MODEL_INFO };
}
